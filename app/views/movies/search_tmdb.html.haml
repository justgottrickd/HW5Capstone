-#  This file is app/views/movies/search_tmdb.html.haml
-# PATRICK WALSH
%h1 Several results containing '#{@result_from_search}' are shown below:

= form_tag :action => 'add_tmdb' do
  %table#movies
    %thead
      %tr
        %th Movie Title
        %th Rating
        %th Release Date
        %th Select
    %tbody
      - @movies.each do |movie|
        %tr
          %td= movie[:title] 
          %td= movie[:rating]
          %td= movie[:release_date]
          %td=check_box_tag "tmdb_movies[#{movie[:tmdb_id]}]"
  = submit_tag 'Add Movies selected above to list', :id => 'tmdb_movies'

= link_to 'Go back to list of movies.', movies_path
